import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

//main server request
//in terminal go to cd to the file page (homework-3-oddahere) and "NPM start" to start the server

//for example "cd desktop -> cd homework-3-oddahere -> NPM start"

//goes from app.js to index.js

ReactDOM.render(
  <App />,
  document.getElementById('root')
);